server {
    listen 3001;
    listen [::]:3001;

    location = / {
      proxy_pass      http://${NTOPNG_HOST}:3002/;
    }

    location / {
      proxy_pass      http://${NTOPNG_HOST}:3002$request_uri;
    }
}
